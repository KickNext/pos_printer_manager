#!/bin/sh
# =============================================================================
# Pre-commit хук для автоматического форматирования Dart-файлов
# Запускается автоматически перед каждым коммитом
# =============================================================================

echo "🔍 Запуск pre-commit проверок..."

# Получаем список изменённых .dart файлов, которые будут закоммичены
STAGED_DART_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep "\.dart$" || true)

# Если нет изменённых .dart файлов - пропускаем
if [ -z "$STAGED_DART_FILES" ]; then
    echo "✅ Нет изменённых Dart-файлов для форматирования"
    exit 0
fi

echo "📝 Найдены изменённые Dart-файлы:"
echo "$STAGED_DART_FILES"

# Форматируем каждый файл
echo ""
echo "🎨 Форматирование Dart-файлов..."

FORMAT_FAILED=0
for FILE in $STAGED_DART_FILES; do
    if [ -f "$FILE" ]; then
        dart format "$FILE"
        if [ $? -ne 0 ]; then
            echo "❌ Ошибка форматирования: $FILE"
            FORMAT_FAILED=1
        else
            # Добавляем отформатированный файл обратно в индекс
            git add "$FILE"
        fi
    fi
done

if [ $FORMAT_FAILED -ne 0 ]; then
    echo ""
    echo "❌ Форматирование завершилось с ошибками"
    exit 1
fi

echo ""
echo "✅ Форматирование завершено успешно"

# Опционально: запуск анализатора (раскомментируйте при необходимости)
# echo ""
# echo "🔬 Запуск dart analyze..."
# dart analyze
# if [ $? -ne 0 ]; then
#     echo "❌ Анализ выявил ошибки. Исправьте их перед коммитом."
#     exit 1
# fi

exit 0
